<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.worldskills.colorapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icono_app_instalada"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Inicio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pantallas.Juego" />
        <activity android:name=".pantallas.Puntajes" />
        <activity android:name=".pantallas.Configuracion" />
        <activity android:name=".pantallas.Resultados"></activity>
    </application>

</manifest>